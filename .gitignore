# macOS
.DS_Store

# Editors
.vscode/
.idea/

# Xcode / iOS
build/
DerivedData/
xcuserdata/
*.xcuserstate
*.xcactivitylog
# Keep workspace tracked, ignore only user data inside it
*.xcworkspace/xcuserdata/

# Swift Package Manager (you can keep Package.resolved tracked)
.swiftpm/
.build/
# If you do NOT want to pin deps, uncomment the next line:
# Package.resolved

# CocoaPods
# Keep the Pods/ directory OUT of git (recommended)
Pods/
# DO track these two:
!Podfile
!Podfile.lock
# DO NOT ignore the workspace (CocoaPods generates it)
!*.xcworkspace

# Carthage (if used)
Carthage/Build/

# Fastlane (if used)
fastlane/report.xml
fastlane/Preview.html
fastlane/screenshots

# Artifacts / archives
*.ipa
*.dSYM.zip
*.mobileprovision

# Local config / secrets
Secrets.xcconfig
*.local.json
.env
*.pem

# Backend (AWS SAM / Node) – applies in any subfolder
**/.aws-sam/
**/node_modules/
# Keep backend manifests
!**/package.json
!**/package-lock.json
!**/pnpm-lock.yaml
!**/yarn.lock
